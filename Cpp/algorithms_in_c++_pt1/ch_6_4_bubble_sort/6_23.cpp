/*
 * stability is a bonus
 * it does less exchanges for any sorted segments whereas selection sort
 * will still do an exchange for each element in a sorted segment if a
 * lower value is at a higher index than the sorted segment
 */