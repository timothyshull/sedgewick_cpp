/*
 * Use that same operation as the previous but using the bottom of the deck as "higher".
 * You can continuously move higher cards to the top and then rotate it to the bottom
 * which will move the sorter modularly through the deck until it is sorted and the top card
 * is the lowest
 */