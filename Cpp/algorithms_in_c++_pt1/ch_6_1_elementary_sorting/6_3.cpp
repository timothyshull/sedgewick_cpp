/*
 * Using the set order just continuously move through, left to right,
 * comparing the first card with all other cards and swapping the cards
 * if one should be on the left more strongly than the other. Once the first
 * card is the correct left card, move on to the next card, etc
 */